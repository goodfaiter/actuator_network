[project]
name = "actuator_network"
version = "0.1.0"
description = "Package for processing mcap files and training an actuator network"
authors = [
    {name = "Valentin Yuryev", email = "valentin.yuryev@gmail.com"},
]
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
dependencies = [
    "torch>=2.0",
    "numpy",
    "pandas",
    "scikit-learn",
    "matplotlib",
    "tqdm",
    "PyQt6",
    "pyarrow",
    "rosbags",
    "mcap",
    "mcap-ros2-support",
    "wandb",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.6.0",
]

[project.scripts]
train-mlp = "actuator_network.train_mlp:main"
train-rnn = "actuator_network.train_rnn:main"
train-transformer = "actuator_network.train_transformer:main"
train-m5 = "actuator_network.train_m5:main"
test-mlp = "actuator_network.test_mlp:main"
test-rnn = "actuator_network.test_rnn:main"
test-transformer = "actuator_network.test_transformer:main"
test-m5 = "actuator_network.test_m5:main"

[project.urls]
repository = "https://github.com/goodfaiter/actuator_network"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
default-groups = ["dev"]

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cu128" }

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "F",   # Pyflakes
    "I",   # isort
    "N",   # pep8-naming
    "W",   # pycodestyle warnings
]

[tool.ruff.lint.pydocstyle]
convention = "google"
